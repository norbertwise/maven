pipeline {

   agent {
        docker { image 'alpine'  }
    }

    stages {
        stage('Test') {
            steps {
                sh 'echo hello form container'
            }
        }
    }
}